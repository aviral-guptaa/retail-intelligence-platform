# Core runtime (demo mode + dashboard work with only these)
numpy>=1.26
opencv-python>=4.9
PyYAML>=6.0
python-dotenv>=1.0
pandas>=2.2
scikit-learn>=1.4
joblib>=1.3
fastapi>=0.110
uvicorn[standard]>=0.29
websockets>=12.0
SQLAlchemy>=2.0
pydantic>=2.6
pytest>=8.0

# Optional: real person detection & edge export.
# Install the lines below when running against real cameras / building YOLO/ByteTrack models.
# torch>=2.2
# torchvision>=0.17
# ultralytics>=8.2
# onnx>=1.16
# onnxruntime>=1.18

# Optional: API tests (TestClient). The tests skip cleanly when absent.
httpx2>=0.1

# Optional: web dashboard video-upload form handling (used by run_web.py).
# Not needed for the headless CLI / core analytics loop.
python-multipart>=0.0.9
