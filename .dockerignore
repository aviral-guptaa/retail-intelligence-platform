.venv/
venv/
__pycache__/
*.py[cod]
.git/
.pytest_cache/
*.db
runs/

# Large data / models are regenerated at build time (see Dockerfile) or ignored.
data/*.db
data/raw/*
data/processed/*
data/uploads/*
# regenerable labelled crops (see scripts/build_shelf_dataset.py)
data/shelf/
models/yolo/*.pt
# the ONNX export ships in the image: COPY . . relies on it being present
# (the fewer-megabyte detector for Render's free tier). Do not exclude it.
*.log
*.zip
.DS_Store